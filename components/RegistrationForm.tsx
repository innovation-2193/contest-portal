"use client";
import { useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
export function RegistrationForm(){const router=useRouter(),params=useSearchParams();const [error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const form=new FormData(e.currentTarget);const payload=Object.fromEntries(form);const response=await fetch("/api/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...payload,provider:params.get("provider")||"local",consentPdpa:form.get("consentPdpa")==="on"})});const data=await response.json();if(response.ok)router.push(`/register/success?code=${data.registrationCode}`);else{setError(data.error);setBusy(false);}}
return <form className="form-card" onSubmit={submit}><div className="form-heading"><span>ข้อมูลผู้เข้าร่วม</span><h2>กรอกข้อมูลลงทะเบียน</h2><p>ช่องที่มีเครื่องหมาย * จำเป็นต้องกรอก</p></div><div className="form-grid"><label className="span-2">อีเมล *<input type="email" name="email" required placeholder="name@example.com"/></label><label>คำนำหน้า *<select name="title" required><option value="">เลือก</option><option>นาย</option><option>นาง</option><option>นางสาว</option><option>ร.ต.ต.</option><option>ร.ต.อ.</option></select></label><label>ชื่อ *<input name="firstName" required/></label><label>นามสกุล *<input name="lastName" required/></label><label>หมายเลขบัตรประชาชน *<input name="citizenId" inputMode="numeric" pattern="[0-9]{13}" maxLength={13} required/></label><label>เบอร์ติดต่อ *<input name="phone" inputMode="tel" pattern="[0-9]{9,10}" required/></label><label>กองบังคับการ *<input name="division" required/></label><label>กองบัญชาการ *<input name="bureau" required/></label></div><label className="consent"><input type="checkbox" name="consentPdpa" required/>ยินยอมให้จัดเก็บและประมวลผลข้อมูลตามวัตถุประสงค์ของโครงการ</label>{error&&<p className="error">{error}</p>}<button className="primary submit" disabled={busy}>{busy?"กำลังบันทึก...":"ยืนยันการลงทะเบียน"}</button></form>}
